# Nixpacks configuration for Railway deployment
# This tells Railway to build and run the signaling server

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd signaling_server && npm install"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd signaling_server && npm start"

